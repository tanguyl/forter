program addNumbers

* This simple program adds two numbers
    implicit none

* Type declarations
    real :: a, b, result

* Executable statements
    a = 12.0
    b = 15.0
    result = a + b
*   result should be 27

end program addNumbers
